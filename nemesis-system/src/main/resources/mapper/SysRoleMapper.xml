<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lsjyy.nemesis.system.dao.SysRoleMapper">
    <select id="selectUsrRole" resultType="String">
        SELECT r.interface_id FROM sys_role s INNER JOIN role_interface r on s.role_id =r.role_id WHERE s.role_name='USER'
    </select>

</mapper>